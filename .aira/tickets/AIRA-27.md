---
{"schema":1,"id":"AIRA-27","project":"aira","title":"Admission reserves but doesn't ENFORCE → slice over-commit OOMs well-behaved confined neighbours (flat oom_score_adj=500)","status":"done","kind":"bug","severity":"P1","assignee":null,"milestone":null,"labels":["admission","confine","dogfood","oom","shared-slice"],"hold":false,"relations":[]}
---
Reported convergently by two dogfooding sessions 2026-09-01 (subpipe/altium inc-4b + money), confirmed by slice telemetry.

ROOT CAUSE: confine admission RESERVES slice RAM but does not ENFORCE that a job stays within its reservation. The `--memory-max N --memory-reserve small` combo reserves `small` in the ledger while the scope's memory.max is set to the explicit N — so the job can balloon to N at runtime while admission only accounted `small`. Σ(actual RSS) then exceeds Σ(reserved) → aira.slice hits its own memory.max (64G) → a SLICE-level OOM fires. Because every confined process is launched with a FLAT `oom_score_adj=500` (confine_linux.go, oomAdj), the kernel's victim selection does NOT prefer the over-subscriber — a WELL-BEHAVED job within its reservation is just as likely (often the unlucky) victim. Its scope's `oom.group=1` then group-kills the whole innocent scope.

EVIDENCE:
- subpipe: a `make merge-gate` under a PROPER whole-job reservation (not the under-reserve anti-pattern) was OOM-killed 5× at the engine leg; a plain liveness probe SURVIVED the same window; engine.log reached ~73% before a slice-wide kill; completed clean on the first try AFTER the slice drained (same reservation). `--list` showed ~63G granted / ~62G ceiling across 20-28 jobs incl. multiple sibling engine gates. ~6h of retries lost.
- money: a LIGHT `aira confine --memory-max 512M -- bash <poll>` using ~10MB died with `scope-integrity=unverified` (NOT descendant-killed = not ITS own scope OOM) — killed externally by the slice-level OOM picking a confined victim. money initially suspected a banned `systemctl stop aira.slice`; REFUTED (slice continuously active since the 19:19 WSL reboot, no stop/watchdog/confine-kill event).
- slice telemetry: aira.slice memory.events `max=1053918` (hit its cap constantly), `oom=83`, `oom_kill=327`, `oom_group_kill=23`. Confine oom_score_adj=500 makes confined jobs the PREFERRED slice-OOM victims.

CLARIFY (both peers conflated it): confined jobs are WATCHDOG-exempt but NOT exempt from the slice's own cgroup memory.max OOM — adj=500 makes them the preferred targets.

FIXES (subpipe's, both sound):
(a) ENFORCE reserve ≈ cap (the ROOT fix): reject or up-charge `--memory-max` > the admission reserve so a scope physically cannot exceed what it reserved (set scope memory.max = the charged reserve, or charge reserve = --memory-max). Then Σ(actual) ≤ Σ(reserve) ≤ cap−headroom by construction — the slice cannot over-commit, so the slice-level OOM cannot fire. Note: a plain `--memory-reserve R` (no --memory-max) ALREADY caps the scope at R (confine_linux.go:588) — the hole is the explicit `--memory-max` override.
(b) RESERVATION-AWARE oom_score (defense-in-depth): scale oom_score_adj by reservation-compliance — a scope over its reserve should be a higher-priority OOM victim than one within it, so the over-subscriber is killed, not the well-behaved neighbour. Replaces the flat 500.

SEVERITY P1: a shared-slice hazard that destroys well-behaved neighbours' long gates under contention (data loss, no queue to notice), triggered by a common anti-pattern. relates: AIRA-24 (the --memory-max-big+--memory-reserve-small under-reserve footgun), AIRA-12/#67 (governed OOM / reserve ledger), AIRA-16 (watchdog). This is the RAM-bound-enforcement piece of the cooperative-scheduler direction.

**CORRECTION (2026-09-01, code-verified — supersedes the ROOT CAUSE + FIX (a) framing above):** the VECTOR was mis-attributed. NON-delegate confine does NOT under-reserve — the CLI UP-CHARGES the admission reserve to `--memory-max` (`main.go:796-798` unconditional `reserve=maximum`; `confine_linux.go:458-461` forces `reserve=scopeMemoryMax` for `!DelegateRAM`, narrowed there by AIRA-15/`6609d16`). So a non-delegate scope is AIRTIGHT (cap == reserve, kernel-hard-capped, self-OOMs, can't over-commit). **Fix (a) is therefore ALREADY IN FORCE for non-delegate** — nothing to build but an OPTIONAL CLI honesty-reject of the contradictory `--memory-max < --memory-reserve` flags. The REAL over-commit source is **DELEGATE-RAM**: a delegate-ram scope charges only ~512MiB overhead to the ledger but its `memory.max` is the 48G scope-ceiling (`confine.go:24-26`, NOT admission-bounded), AND its per-test gate FAILS OPEN under contention (`aira_xdist_governor/__init__.py`: reserve-timeout/error → runs UNGOVERNED). So Σ(delegate scope RSS) is not bounded by admission → the slice over-commits → slice-OOM → the flat `oom_score_adj=500` (aira.slice has NO oom.group) kills a per-task victim by ~RSS. Fix (a) CANNOT cover delegate-ram (would revert AIRA-15's per-test model). So the load-bearing fix is **(b) reservation-aware oom_score** (daemon per-pid re-writer: over-grower = higher adj victim, well-behaved = protected) OR bounding the delegate aggregate (dynamic scope cap / fail-closed gate) — owner chose to spec fix (b) via the two-loop, owner-gated. The swap bump (2→8G, `43ab203`) is defense-in-depth. The CORE hazard (over-commit → slice-OOM → flat-adj collateral), the P1 severity, and subpipe's/money's evidence all STAND; only the VECTOR is corrected (delegate-ram, not non-delegate --memory-max).

**DONE + DEPLOYED (2026-09-01, `2e8f237`, master `7f1d9fc`):** fix (b) Option A shipped via the full two-loop (spec v2 → Sol/DeepSeek/Fable plan-review → Terra build → adversarial build-review 0-confirmed → Opus verify → confined `make ci` exit 0 → live). Class-based `oom_score_adj` in `confine_linux.go`: non-delegate confine stays **500** (airtight, the protected class), `--delegate-ram` becomes **800** (the over-committing class → now the preferred slice-OOM victim); env overrides `AIRA_CONFINE_OOM_SCORE_ADJ`/`_DELEGATE` validated + rejected (`E_CONFINE_ARGUMENT_INVALID`) outside `[500,1000]` or if delegate≤non-delegate. Fixed the porous `runConfineTestSetup` fixture (was hardcoding `500\n`; now writes the parsed value). Live smoke verified: non-delegate confine descendant reads `oom_score_adj=500`, `--delegate-ram` reads `800`. Client/runner-side only → NO daemon restart. Paired with the swap 2→8G shock absorber (`43ab203`) + skill guidance correction (`22562e5`). All 11 fastest.ee sessions notified. **HONEST LIMITATION (documented in the plan):** a strong BIAS, not a guarantee — reliably protects LIGHT airtight jobs + biases hard toward delegate victims, but a LARGE single airtight process (subpipe's 32G gate, spice's cargo builds) can still be outscored by a small delegate worker's 300-pt delta. **NEXT MILESTONE (owner-directed):** the STRUCTURAL fix — bound the delegate aggregate so no slice-OOM fires at all (dynamic scope cap / fail-closed per-test gate). Option B (dynamic per-scope reservation-compliance re-writer) deferred; blocked by anonymous per-test leases (see plan §2, AIRA-25/26).
