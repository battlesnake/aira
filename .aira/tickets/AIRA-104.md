---
{"schema":1,"id":"AIRA-104","project":"aira","title":"aira confine job resource profile: surface durable peak-RSS (already captured) and add CPU-time/pids-peak, for exclusive-mode benchmarking","status":"done","kind":"feature","severity":"P2","assignee":null,"milestone":null,"labels":["confine","dogfood","observability"],"hold":false,"relations":[]}
---
Direct owner request (2026-09-05), motivated by AIRA-101 (`aira confine --exclusive`): after an exclusive run finishes and the slice goes back to empty, make it easy to get a resource-usage measurement for the WHOLE job — peak RSS and other things — covering every subprocess it spawned over its life, not just its own top-level process. The stated purpose: exclusive mode should be usable to measure the total resource requirements of a job that may fork many short-lived subprocesses during its run.

## Good news: most of the foundation already exists — this is smaller than it looks

Traced against current source before writing this ticket, rather than assuming a from-scratch build is needed:

- **Peak RSS is already correctly harvested from the right primitive.** `internal/runner/usage_linux.go` already reads `<scope>/memory.peak` — cgroup v2's own high-water-mark of `memory.current` for the WHOLE cgroup subtree. This is exactly right for "a job that spawns many subprocesses": it is a kernel-maintained running maximum across every process ever charged to that cgroup, including ones that have already exited, with no polling and no risk of missing a spike between samples. `internal/runner/types.go`'s `ConfineStatus.PeakRSS *int64` already carries it, and `internal/runner/confine_linux.go`'s reserve-sizing advisory already reads it (`formatConfineReserveAdvisory`).
- **The durable-record machinery to query it after the fact already exists.** AIRA-22's `ConfineDetachRecord.Status *ConfineStatus` (`internal/runner/confine_detach.go`) already embeds the whole `ConfineStatus` struct — including `PeakRSS` — into the durable per-job record `aira confine --status` reads back after a detached job finishes. AIRA-101 already extended this same struct with its own `Exclusive`/`ExclusiveDrainedMS` facets, so this is an actively-maintained pattern, not a stale one.
- **Confirmed live on this kernel** (6.18): `memory.peak`, `cpu.stat` (`usage_usec`/`user_usec`/`system_usec`), and `pids.peak` are all present and readable on a real slice cgroup here.

## What's actually missing

1. **`aira confine --status`'s human-readable renderer does not print `PeakRSS` at all**, even though the durable record already carries it (`grep` for "PeakRSS"/"peak_rss" across `cmd/aira/*.go` finds zero matches). The data has been sitting in the record, unsurfaced, since AIRA-22. This is the most direct, cheapest fix: render what's already captured.
2. **No CPU-time capture exists anywhere.** `cpu.stat`'s `usage_usec` (total CPU time, user+system, for the whole cgroup subtree — the CPU analogue of `memory.peak`, same subprocess-churn-proof property) is not read, not stored, not rendered. Add it following the *exact* pattern `PeakRSS` already established: read in `usage_linux.go` at the same teardown point, add a field to `ConfineStatus`, carry it through the durable record, render it on the trailer and in `--status`.
3. **`pids.peak`** (peak concurrent process count in the subtree) is a natural, cheap third addition — "how parallel did this job get" — same read-once-at-teardown pattern. Include it unless the plan finds a reason not to; it's not load-bearing like the first two.
4. **Tie it to the exclusive-benchmarking story explicitly.** When `Exclusive == "granted"`, the resource numbers on the trailer/`--status` output are the number the owner actually wants — make sure the presentation makes that connection obvious (e.g. grouped with or immediately following the `exclusive=` facet), rather than leaving the caller to correlate two separate pieces of output themselves. This is a rendering/UX question for the plan, not a new mechanism.

## Design questions the plan should resolve

- **Where exactly is "teardown," precisely?** These cgroup files vanish the instant the scope directory is removed. Confirm the existing `PeakRSS` read already happens at the correct point (immediately before scope teardown, while the cgroup still exists) and add the new CPU-time/pids-peak reads at that exact same point — do not introduce a second read timing that could race the first.
- **Delegate-ram / nested-worker jobs.** For a `--delegate-ram` aitest run, is the outer confine scope's own `memory.peak`/`cpu.stat` already the correct whole-job aggregate (cgroup v2's hierarchical accounting should mean yes — a parent's `memory.peak` reflects its whole subtree, worker sub-scopes included), or does something about how workers are drained into a sub-scope (the `.aira-supervisor` drain pattern from the AIRA-91 investigation) complicate this? Verify against source rather than assume either way.
- **Honesty on read failure.** If a stat file can't be read (older kernel lacking `memory.peak`/`cpu.stat` — some of these are recent additions, or a genuinely-vanished scope), the existing `PeakRSS` pattern already does this right (`*int64`, nil means unestablished, rendered as absent/unevaluated never a fabricated zero) — follow the identical discipline for the new fields, don't invent a different failure convention.
- **`--json` output.** Confirm the JSON record already carries `peak_rss` (it should, per the struct tag) and that the new fields get equivalent `omitempty` JSON tags, consistent with every other optional facet on this struct.

## Not exclusive-only, but exclusive is what makes it trustworthy

This capability is generically useful for any confine job (a plain `aira confine -- make merge-gate` benefits from knowing its own peak RSS/CPU time just as much). What AIRA-101's exclusive mode adds is confidence that the number reflects the job alone, uncontended — worth stating in whatever documentation/help text describes this, so a caller understands why an exclusive run's numbers are meaningful in a way a contended run's aren't.

## Relation to other tickets

Builds directly on **AIRA-101** (the motivating use case, and the struct/pattern this extends) and **AIRA-22** (the durable-record/`--status` machinery this surfaces the new data through). Not related to AIRA-102/AIRA-103.

## Resolution

Built, reviewed, and merged 2026-09-06. Classified build-small per the brief (no daemon/admission/ledger changes) — self-review + one independent code-reading pass, not the full two-loop.

**What was built** (all in `internal/runner`, plus one doc sentence each in `internal/core`):

- `ConfineStatus` (confine.go) gained `CPUUser`/`CPUSys *int64` beside the existing `PeakRSS`, named to mirror `RunRecord`'s existing convention rather than a single combined usage field.
- **The build_notes above were partly stale by the time work started** (explicitly flagged as a risk in the brief, and confirmed against current source before touching anything): `internal/runner/usage_linux.go`'s `readCgroupUsage`/`cgroupUsage` already parsed `cpu.stat` into `CPUUser`/`CPUSys` fields, landed by an unrelated intervening change since this ticket was filed. So step 2's "no CPU-time capture exists anywhere" was no longer true — only the *propagation* onto `ConfineStatus` and the *trailer rendering* were actually missing. `confine_linux.go`'s single existing teardown read (`usage := deps.readUsage(...)`) now also assigns `usage.CPUUser`/`usage.CPUSys` onto `result.Status`, right next to the pre-existing `PeakRSS` assignment — no second read introduced.
- `FormatConfineStatus` renders `peak-rss=<FormatConfineBytes(...)>` and `cpu=<user>+<sys>` (exact, non-rounded durations via a new `formatConfineCPUUsec` helper) when non-nil, else `unevaluated` for each — placed immediately after the `exclusive=`/`drained-for=` block so a granted exclusive run's resource numbers sit next to its exclusivity attestation. Always rendered, not gated on `--exclusive`, on the same honesty discipline as `terminated-by=`.
- `pids.peak` deliberately **not** added, per this ticket's own recommendation (already documented elsewhere in the codebase as misleading: counts TIDs, inflated by leader threads and AIRA's own in-scope setup helper).
- Updated the one literal golden-string trailer test that changed shape (`TestFormatConfineStatusUnchangedWithoutContainer`), plus 6 new unit tests: unevaluated-on-nil, rendered values, a half-established CPU read renders the whole facet unevaluated (never fabricates the missing half), facet placement immediately after `exclusive=`, and that CPUUser/CPUSys (including a genuine zero — deliberately **not** clamped the way PeakRSS's own `<=0` clamp is) reach `result.Status` from the same teardown read.
- One sentence added to `core.go`'s `--exclusive` flag help text and to `skill.go`'s generated confine section: these are whole-subtree hierarchical cgroup counters (aitest worker sub-scopes, podman `--cgroups=split` children included); Docker containers (AIRA-102, structurally outside the slice) are **not** counted.

**PR / merge**: https://github.com/battlesnake/aira/pull/52, merged as merge commit `2e59e76b42838de357dc7046d24147fdb4df5b81` into `master`. Independently re-verified after merge: `git diff` of the merge commit's tree against the PR branch's final pushed tip (`e3771da214366876ac69a460feb0135f678eb22a`) for every touched file is empty — the merged content is byte-identical to what CI validated, not a silently-dropped fix.

**Exact verification exit codes** (every heavy command under `aira confine --`):
- `go build ./...`: 0 (checked after every rebase, 4 times across the build).
- `go vet ./...`: 0.
- `gofmt -l` on every touched file: clean.
- `go test ./internal/runner/... ./internal/core/...` (targeted): 0, repeatedly.
- `go test ./... -race ./internal/runner/... ./internal/core/...` (local, pre-push): 0.
- `go test ./...` (full suite, exit 0 each time): run **6 times** total across the build via the repo's own `aira confine`-wrapped pre-push hook (`make ci`) plus two manual full runs — every one of the 14 tested packages `ok` (`internal/cgrouptest` has no test files), zero FAIL/panic, **except one transient environmental flake**: `TestM20bSupervisorRejectsMalformedSidecarBeforeOpeningProject` (package `cmd/aira`, untouched by this diff) failed once with an EOF during a run where the daemon log showed a concurrent cross-session `confine-kill --steal` and scope-reaper activity from OTHER sibling sessions on this shared machine at the same moment. Re-ran that single test in isolation 10/10 green immediately after; the very next full-suite push-hook run (no code change in between) was clean. Treated as an environment-contention flake, not a regression — not touched by this diff, and this machine had 5-7 other confine jobs from sibling sessions running concurrently throughout the build (see the concurrently-active AIRA-108 investigation, an unrelated but confirmed real contention-era bug in `cmd/aira`'s `confine-reserve --pinned` wait-bound enforcement, for independent evidence the box was genuinely under multi-session load at the time).
- GitHub Actions CI (`build + vet + gofmt`, `test`, `race` — the `race` job landed on `master` via AIRA-20 mid-build, picked up automatically by the PR's checks since `pull_request` runs use the base branch's current workflow definition): all three SUCCESS on the final merged commit.

**Open design questions from the brief, how resolved**:
- *Teardown timing*: confirmed the existing `PeakRSS` read already happens at the correct point (immediately before scope teardown, while the cgroup still exists); the new CPU fields ride the exact same read, so no new race was introduced.
- *Delegate-ram / nested aitest workers*: confirmed via source (`internal/runner/worker_scope.go`'s `WorkerScopeChildPath`, `filepath.Join(parent, ".aira-"+id)`) that worker sub-scopes are real cgroup **children** of the outer confine scope's directory, so cgroup v2's own hierarchical `memory.peak`/`cpu.stat` accounting on the outer scope already aggregates them correctly — no extra plumbing needed.
- *Honesty on read failure*: CPUUser/CPUSys follow the identical `*int64`-nil-means-unestablished discipline as PeakRSS, but deliberately **without** PeakRSS's `<=0` clamp — a real, very short-lived or I/O-bound job can legitimately show 0 user or system usec, whereas `memory.peak == 0` for a cgroup that ran a real process is itself suspicious. If only one of the pair is nil (a defensive case; `cpu.stat` always emits both keys together in practice), the whole `cpu=` facet reads `unevaluated` rather than fabricating the missing half.
- *`--json`*: both new fields carry `omitempty` json tags consistent with every other optional facet on the struct; `confine --status --json` marshals `ConfineStatus` directly (via `ConfineDetachRecord`), so no additional wiring was needed.
- *pids.peak*: not added, per this ticket's own recommendation.

**Review**: self-review, then one independent code-reading pass by codex-sol (GPT-5.6-Sol) on the full diff — verdict **APPROVE-WITH-NITS**. One P3 finding: the usec→`time.Duration` multiply in `formatConfineCPUUsec` can in principle int64-overflow above ~292,471 years of accumulated CPU time. Addressed with a documenting comment (not reachable by any real cgroup) rather than added complexity, consistent with this project's architectural-simplicity bar. Sol found no missed production `ConfineStatus` construction site and no missed golden-string test beyond the one already fixed.
