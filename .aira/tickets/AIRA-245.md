---
{"schema":1,"id":"AIRA-245","project":"aira","title":"confine ledger has no worker-headroom admission-control for concurrent --delegate-ram parents: a delegate-ram parent holds its framework-overhead reserve, but its per-test worker sub-reservations still face the full capacity gate (admit.go:2223, no carve-out — the sub-reservation exemption at :543-553 covers only the exclusivity/drain gate). So a pathologically over-subscribed set of concurrent NON-exclusive --delegate-ram legs (Σ overheads + fixed leaves fills the slice, no worker room, nothing finishing) can DEADLOCK — every parent blocks on its own first worker under the unbounded (no-timeout) admission wait. Realistic configs converge (small overheads, few legs, finishing leaves); surfaced by deploy's CI-gate-architecture (each heavy leaf self-confines against one ledger). Consider: reserve worker headroom per admitted delegate-ram parent, or an admission-control on concurrent-parent count, or a deadlock detector on all-parents-blocked-on-own-workers.","status":"planned","kind":"bug","severity":"P3","assignee":null,"milestone":null,"labels":[],"hold":false,"relations":[]}
---

