---
{"schema":1,"id":"AIRA-25","project":"aira","title":"Confine admission class-split + per-test delta tightening (removes Σ(file-resident) over-charge)","status":"done","kind":"feature","severity":"P2","assignee":null,"milestone":null,"labels":["admission","confine","daemon","deferred"],"hold":false,"relations":[{"kind":"relates","from":"AIRA-25","to":"AIRA-26"}]}
---
DEFERRED (owner chose to stop at per-worker-admission Slice 1, 2026-09-01). This was "reshaped Slice 2" after the plan-review dissolved the per-worker-base concept (see AIRA-26).

WHAT: split the confine admission ledger into PEAK-class (whole-job reserves, kept in the existing `outstanding`, charged via `max(effectiveCurrent, Σpeak+adopted)`) and DELTA-class (per-test forward increments, a new `deltaOutstanding`, charged as `+ Σdelta`), and change the per-test reserve from Slice-1's cumulative `max(marker, RSS+pad)` to the INCREMENT `max(marker, RSS+pad) − B` (B = the worker's frozen post-import/post-trim resident baseline). So `charge = max(effectiveCurrent, Σpeak+adopted) + Σdelta`.

WIN (modest): Slice 1 over-charges by `Σ(RSS_i) − effectiveCurrent ≈ Σ(file-resident_i)` (~0.5–2GiB at -n16) because each worker's mapped-file pages are in its statm RSS but discounted out of effectiveCurrent by AIRA-21. The delta form uses `current` (cache-discounted) + growth-only deltas, removing that over-charge; also tightens marked tests. NO per-worker base lease (dropped — it is a no-op, AIRA-26).

VERIFIED (Fable, code-grounded 2026-09-01): the class-split arithmetic is a SOUND non-OOM bound (monotone grant argument preserves Σreserve ≤ cap−headroom; delta double-count of materialised growth is bounded — exactly one delta lease per worker at a time — and strictly over-reserve, never OOM). Classifier gaps to fix if built: (1) `admitWaiter` carries no class field; (2) `confine-reserve` requests carry no class (delegate_ram is set only on the whole-job scope admission) → add a `--class peak|delta` tag; extend `checkedAvailable` (2 prod callers admit.go:209,716 + ~6 test sites), the headroom-job counters (admit.go:207/213/428/557/714), and the #73 `--list` reserve summary to surface Σdelta.

WHY DEFERRED: Slice 1 already delivered the big everyday false-block win; this is a modest tightening for a DAEMON-RESTART change — low ROI per architectural-simplicity. Revisit if field data shows the file-resident/marked over-charge is still causing blocks. relates: Slice 1 (master 9af4a67), AIRA-26, #67/#69/#74.

## Resolution (2026-09-05, backlog-completion triage)

**Superseded by:** AIRA-30/AIRA-39 (landed: aitest per-worker memory.max ledger in internal/daemon/worker_admit.go replaces the per-test lease model) + AIRA-29 (owner-chosen track-actual ledger direction; its body already records AIRA-25 as SUBSUMED) + AIRA-33 (deletes aira_xdist_governor and the confine-reserve call sites, i.e. AIRA-25's subject)

VERIFIED UNBUILT: no `deltaOutstanding`, `--class peak|delta`, or any reserve-class symbol exists anywhere in the real tree (grep over *.go/*.py, excluding the stale .claude/worktrees copies). `checkedAvailable` at /home/mark/claude/aira/internal/daemon/admit.go:1729-1746 is still the single-class form `charge = max(effectiveCurrent, outstanding+adopted)` (adopted via addClamp at :917 and :1623). So the ticket's "not yet built" framing is accurate for the code itself.

WHY IT IS MOOT (architecture changed underneath it): the over-charge AIRA-25 removes exists ONLY because the legacy xdist governor sizes each per-test `aira confine-reserve` as cumulative statm RSS + pad (/home/mark/claude/aira/internal/pylib/aira_xdist_governor/__init__.py:246-269, issued at :353) and those N leases are summed into `queue.outstanding`. That model has been replaced for governed suites by aitest (AIRA-30 done+deployed 2692053; ledger rewritten as the real cgroup tree by AIRA-39/41/63 f0a6283): worker RAM is charged as Σ memory.max of `.aira-worker-*` children against the OUTER scope's own ceiling (worker_admit.go:83-102, 480-557, with the same subtractFloor cache discount at :487), and `worker_admit.go` contains zero references to `outstanding`/`sliceQueue` — it never touches the slice ledger (the generated SKILL text at internal/core/skill.go:322 states "worker-admit adds no slice-ledger charge of its own"). Per-worker committed caps are not statm-RSS sums, so the Σ(file-resident_i) over-charge cannot arise in the aitest model at all; the SKILL guidance already tells projects to `-p no:aira_xdist_governor`.

RESIDUAL LIVE PATH, honestly stated: the xdist governor is still in the tree and still go:embedded (internal/pylib/extract.go:37), and one fastest-ee leg (`test-lite-slowbuild`, plain xdist `-n auto` under the governor per AIRA-100) still exercises the per-test lease path. But that entire stack is scheduled for deletion by AIRA-33 (still `planned`; its precondition AIRA-91 Part A is now confirmed live per commit a603b2a), and the simplification-programme plan (docs/superpowers/plans/2026-09-04-simplification-programme-plan.md:217, :490, :870) CUTs the `confine-reserve` verb itself, explicitly noting it "removes AIRA-25's subject". A class-split built now would be dead code the moment AIRA-33 lands.

THE TICKET'S OWN REVISIT TRIGGER did fire, but pointed elsewhere: the 2026-09-04 live blocking evidence on AIRA-29 (two non-delegate merge-gates each holding a ~26GiB CAP while using 19-21GiB RSS; a 4G waiter queued 28+ min against ~1.5GiB ledger headroom with ~20GB physical free) is whole-job cap-vs-live over-provisioning, which is AIRA-29's track-actual problem, not AIRA-25's per-test file-resident delta. No field report attributes any block to the file-resident per-test over-charge. AIRA-29 charges live memory.current + margin per scope, which removes this over-charge structurally without any per-test class tagging.

PRIOR DISPOSITIONS CONCUR (four independent artifacts already reached this): AIRA-29 ticket body ("AIRA-25 (ledger split — now SUBSUMED)"); the Fable backlog sweep (docs/superpowers/reviews/2026-09-04-fable-backlog-simplification-sweep.md:169, "close as superseded by the already-planned xdist-stack retirement"); the backlog-remediation plan (:78-84, :958-962, "no separate action ... would be wasted, throwaway effort if actioned now"); AIRA-33 ticket :40. Building it would also be a daemon-restart admission-ledger change (correctness-critical, full two-loop) for a self-described "modest" ~0.5-2GiB win at -n16 on a 64GiB slice — squarely what the owner's HARD architectural-simplicity rule forbids. No independent owner fork remains on AIRA-25 itself; the only open decisions (when to land AIRA-29, when to land AIRA-33) belong to those tickets.

RECOMMENDED RESOLUTION NOTE: "Closed superseded 2026-09-05. Subject (per-test confine-reserve leases sized from cumulative statm RSS) replaced for governed suites by aitest's per-worker memory.max ledger (AIRA-30/39, worker_admit.go, no slice-ledger charge) and scheduled for deletion with the xdist stack (AIRA-33, which also cuts the confine-reserve verb). The residual ledger-over-charge concern lives in AIRA-29 (track-actual), which already records this ticket as subsumed and where the only live field evidence of blocking actually points. AIRA-26 remains open independently."


*Disposition: Closed — superseded, reached via a source-verified triage pass (Fable model) as part of the backlog-completion push, independently spot-checked by the coordinating session before closing.*
