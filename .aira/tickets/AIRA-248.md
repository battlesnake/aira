---
{"schema":1,"id":"AIRA-248","project":"aira","title":"GPU/VRAM as a managed admission resource + a GPU-exclusive mode. Extend the confine admission model (today: RAM via the reserve ledger, CPU via the 2×NumCPU core ledger) with a THIRD resource dimension: GPU access gated by VRAM (a per-job VRAM reserve admitted against a machine-wide VRAM ledger, analogous to the RAM reserve ledger), PLUS a GPU-EXCLUSIVE mode (a single job holds the GPU(s) exclusively) mirroring the existing slice-drain/--exclusive mechanism. Owner (2026-09-14): 'another resource we may have to manage is GPU access — gated by VRAM, but also with an exclusive mode similar to what we already have for draining the slice.' Open design Qs for later: per-GPU vs aggregate-VRAM ledger; multi-GPU placement; how VRAM peak is measured (no cgroup equivalent — nvidia-smi/NVML?); interaction with the conjunctive {RAM,CPU} fit (becomes {RAM,CPU,VRAM}); the GPU-exclusive vs slice-exclusive relationship.","status":"planned","kind":"feature","severity":"P3","assignee":null,"milestone":null,"labels":["backlog"],"hold":true,"relations":[{"kind":"relates","from":"AIRA-268","to":"AIRA-248"}]}
---

