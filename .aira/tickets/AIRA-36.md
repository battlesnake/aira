---
{"schema":1,"id":"AIRA-36","project":"aira","title":"Recursive orphan-scope reaper: #72 reaper must reap NESTED scope subtrees (single-level rmdir fails on nested orphans)","status":"done","kind":"bug","severity":"P2","assignee":null,"milestone":null,"labels":["aitest","cgroup","confine","daemon","reaper"],"hold":false,"relations":[]}
---
Found 2026-09-01 via the kill-rate monitor investigation (the OOM burst was benign contained self-OOM; the dig turned this up). PREREQUISITE for aitest's nested-scope model.

BUG: the #72 orphan-scope reaper (`internal/daemon/confine_reaper.go` → `runner.ReapOrphanedConfineScopes`) removes an orphaned empty scope with a SINGLE-LEVEL `unix.Unlinkat(AT_REMOVEDIR)`, which the kernel refuses on a NON-EMPTY cgroup. So any confine scope that has leftover CHILD cgroups when its supervisor dies (crash / OOM-kill / SIGKILL — paths that never run cleanup) orphans the WHOLE TREE permanently. Verified live: 32 lingering dead scopes, 31 were aitest `.aitest-worker-test`-nested trees (dead supervisor, 0 procs, up to 46min old) the reaper could never sweep; 0 plain-leaf orphans (the reaper handles those fine — it's specifically the nested case). Recursive `rmdir` child-then-parent cleans them trivially (proven; manually cleaned all 31 with a per-node dead-supervisor+empty-subtree liveness gate, correctly skipping 2 with live supervisors).

WHY LOAD-BEARING FOR aitest: aitest's outer scope ALWAYS has `.aira-supervisor` + N `.aira-worker-*` children at run death, and its design (spec §3.6) treats worker/supervisor OOM-death as NORMAL — those paths never self-clean. aitest confirmed (2026-09-01): the reaper must be the authoritative backstop, not supervisor self-cleanup (which is best-effort on graceful exit only). Without this, every crashed aitest run litters the slice permanently → unbounded dead-cgroup-tree accumulation.

FIX: make the reaper subtree-aware — reap empty+dead DESCENDANT cgroups deepest-first before the parent, applying the same #72 positive-proof gates per node (all-descendants-empty + supervisor-dead(ESRCH) + age≥2m + !Pending + no live admit lease). Keep the parent-fd-anchored AT_REMOVEDIR (kernel rmdirs only genuinely-empty cgroups → race-safe: a proc appearing mid-sweep just fails that node's rmdir, no force). Correctness-critical → full two-loop. relates AIRA-30 (aitest Slice 1 dependency), #72 (the reaper being extended), #20/#70 (scope machinery). Owner-aware; driving 2026-09-01.

**PLAN v2 REVIEWED + BUILDING (2026-09-01, branch `aira36-recursive-reaper` @ `ed16222`, spec docs/superpowers/specs/2026-09-01-aira36-recursive-reaper-plan.md).** Sol+Fable plan-review (both BLOCK/GATE-FAIL, direction SOUND) → 5 MF folded → v2. Key corrections that SIMPLIFIED the design: (1) threat model was wrong — AT_REMOVEDIR can't kill a worker (kernel EBUSY on populated cgroups, no kill path); the real hazard is naive recursion stripping empty SIBLING branches. (2) Replaced the recursive per-node proof with ONE atomic `cgroup.events populated` read via the existing `scope.Empty()` (per-node cgroup.procs isn't atomic under child→child migration; codebase precedent at confine_manage_linux.go:340-345). (3) Fixed the porous Test 2 — needs an EMPTY SIBLING that must SURVIVE (a lone-live-child test passes naive recursion too). (4) Honest race post-condition (not strict all-or-nothing once removal starts) + honest TOCTOU (O_NOFOLLOW ≠ rename-proof; kernel guard prevents false reap). (5) fd-anchored O_NOFOLLOW enumeration pinned (no walk helper exists). Terra building (kheobsy3b); then confined make ci + -race + adversarial build-review + Opus verify → owner-gated daemon-restart deploy.

**DONE + DEPLOYED (2026-09-02, master `70a87b8`, branch `aira36-recursive-reaper` merged).** Full two-loop: Sol+Fable plan-review (GATE-FAIL → v2, direction sound; the review corrected the threat model — AT_REMOVEDIR kernel guard is the never-remove-live backstop, not a precheck — and replaced the recursive proof with one atomic `cgroup.events populated` read via the existing `scope.Empty()`) → Terra build (`c170fac`) → Opus fixed the tmpdir flag-fixture + mutation-hardened the order-porous discriminator (`6148e63`; 6 empty siblings so a Phase-1 regression is caught order-independently — mutation-verified: bypassing the proof strips empty2 and FAILS) → Fable build-review: **product reaper code CLEAN on all 4 dimensions** (false-reap safety, fd-anchoring, fd ownership, scope boundary), 1 P1 = test-infra leak (`removeScopeTree` depth-1-only → nested test trees leaked onto the live slice + failed AIRA_REAL_CGROUP=1) FIXED recursive (`d4833a6`) + 46 leaked `.aira-test-*` trees swept → Opus verify PASS → `make ci` exit 0, `-race` clean. DEPLOYED: binary swapped + `systemctl --user restart aira-daemon.service` (coordinated with aitest for a clean window; slice quiet, 0 in-flight scopes disrupted); backup `~/tmp/aira-backup-pre-aira36-*`. Live smoke: a synthetic nested orphan (dead pid, `.aira-worker-smoke` child, age 5m) created + reaper-sweep confirmation polling (bszxk6gey). Design: `reapEmptyConfineScopeTree` = Phase-1 atomic `cgroup.events` empty-proof + Phase-2 fd-anchored O_NOFOLLOW post-order removal, depth-bound 32, no kill path, plain-leaf byte-identical. Unblocks aitest's nested per-worker scopes (AIRA-30). Accepted nit: a genuine reap error (depth-overflow >32 / bad read) folds into Skipped silently — documented, not surfaced (pathological, safe-direction). relates AIRA-30, #72, #20/#70.
