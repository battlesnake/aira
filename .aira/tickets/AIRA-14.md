---
{"schema":1,"id":"AIRA-14","project":"aira","title":"AIRA-11 residual: intermittent -n auto bootstrap blank under PEAK ambient load — CPU-side (nice-\u003e5 + aging)","status":"done","kind":"bug","severity":"P2","assignee":null,"milestone":null,"labels":[],"hold":false,"relations":[{"kind":"relates","from":"AIRA-17","to":"AIRA-14"}]}
---
Follow-up to AIRA-11 (ionice idle->best-effort-7, DONE+deployed 2026-08-29). That fix is confirmed NECESSARY but NOT SUFFICIENT: various' n=2 field data on the best-effort-7 binary — Run 1 (Part A engine) ran fully 16869 passed/71m under ambient multi-session load, no blank; Run 2 (Part B gate engine leg) BLANKED exit 5 'no tests ran in 2.18s' zero tracebacks (the AIRA-11 signature, not a collection error), SAME binary, SAME ambient/no-self-contention conditions. So best-effort-7 RAISED the bootstrap threshold (un-starved the import I/O that idle-class zeroed) but the blank recurs INTERMITTENTLY under higher ambient load. Residual is CPU-side: execnet bootstrap interpreter spin-up + import EXECUTION still starving at nice 19 under peak contention. FIX = the nice side: nice 19->5 for the young/bootstrap window with age-decay back to 19 (so long jobs still yield the desktop) — exactly the nice->5 + aging in the owner's cooperative-scheduler design. Decision pending (owner): pull priority-aging forward as a focused standalone fix (supervisor/cgroup-driven, keep the flock) OR fold into the full cooperative-scheduler milestone. Interim: -n auto stays default (best-effort-7 makes bootstrap much more likely), blank-leg detector is the fail-loud backstop (no silent 0-coverage pass). NB the ticket lifecycle has no reopen path (done -> in-progress refused), so this residual is tracked as a linked follow-up rather than reopening AIRA-11.
