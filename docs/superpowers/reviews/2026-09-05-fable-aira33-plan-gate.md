Verification complete — every claim in the plan was checked against source. Here is the gate verdict.

---

VERDICT: **GATE-PASS-WITH-CONDITIONS**

The deletion is safe as scoped. No finding blocks it. Four plan-text corrections are conditions because they would otherwise corrupt the build's own evidence trail (§9.2's record-the-failure step, a duplicate test, an uncompilable step ordering, a silently dropped coverage guard).

## BLOCKING

None.

## NON-BLOCKING (ranked; the first four are the CONDITIONS)

1. **§5.6.3 overclaims "fails against master".** New test 3 (raise still publishes, no wake side-channel) **passes** on master: `evaluateSliceCeiling` guards `deps.signalGovernor != nil` (sliceceiling.go:421), so a fake deps with a nil signal compiles and publishes the raise on master exactly as after the change. Tests 1 and 2 genuinely fail against master (skill.go:322 contains `aira_xdist_governor`; env.go:161–187 exports the nine keys). The plan must state test 3 is a forward-only regression guard and exclude it from §9.2's "record the failure" step — otherwise step 9.2 forces the builder to fabricate or fudge a failure record.
2. **§5.2 retarget produces a duplicate test.** `TestExtractPyLibIsIdempotentAndReadyLast` (extract_test.go:188) is line-for-line identical to the already-existing `TestExtractAitestIsIdempotent` (extract_test.go:161) apart from the function called. The claim "it is the only test of the real end-to-end extraction/idempotence contract" is false. Delete it; do not retarget.
3. **Deleting `TestReleaseAdmitWaiterOnlySignalsGovernor` (admit_test.go:933) silently drops the only guard on the release→queue kick.** The retained `queue.signal()` at admit.go:1809 is asserted nowhere else — the release e2e tests set `admitPollInterval = 5ms` (admit_release_e2e_test.go:99,197), so they pass on the ticker alone. Add one assertion that `afterAdmitRelease` kicks the queue (observable via `queue.kick`), or write the gap down as accepted; otherwise a future refactor degrading release-to-grant latency to 250 ms polling passes green.
4. **§9 step 3 is not compilable as written.** governor.go itself references `s.governor` (governor.go:649, 671, 695, 716), so removing the `Server.governor` field (server.go:94) before deleting governor.go breaks the package. The plan's own bar is "each step compiling": restate steps 3–4 so call-site removal (sliceceiling.go:421/447, admit.go:1813, server.go:296–304/461–463/688) lands in step 3 and the field/construction removal lands in the same commit as the governor.go deletion.
5. **Strip-set shrink residual, worth one sentence in the plan.** Shrinking `StripGovernorEnvironment` to one key stops scrubbing stale inherited legacy coordinates (`AIRA_PY_LIB` etc.) from confine/run children — e.g. a child launched from inside a still-running pre-deletion delegate job would carry a live `AIRA_PY_LIB` pointing at a still-extant extraction dir into a fastest-ee conftest (fail-open guarded, so no breakage, but a real stale-plugin import path). Keeping all ten keys in the strip set while exporting only the scope id costs nothing and removes the corner. The gate-command identity change (gate_command.go:169) is confirmed harmless — it operates on an explicit `EnvAllow` list, so only a gate command that allow-lists a governor key changes digest; the no-compat rule covers it.
6. **Stale prose in retained code the plan missed** (same defect class as its own §3.11): (a) `internal/pylib/aitest/worker.py:305–315` — the AIRA-37 fork-handler audit docstring names `aira_xdist_governor` as AIRA's one real `os.register_at_fork` registrant and repeats the `-p no:aira_xdist_governor` guidance, false post-deletion; (b) `internal/runner/confine_reserve.go:67` "the pytest governor treats as fail-open"; (c) `internal/runner/confine_linux.go:985` and `:1012` comments naming the RAM governor and "the governor's runtime directory"; (d) `internal/daemon/confine_reaper.go` AIRA-68 comment "now the majority of the ledger by count on a busy box" — falsified once per-test reservations stop; (e) `internal/core/skill_test.go:524–528` comment block; (f) the names `governorEnvironmentKeys`/`StripGovernorEnvironment`/`StripGovernorEnv` become lies with one non-governor key left — rename.
7. **§3.8 signature cleanup is incomplete as listed.** `AppendConfineChildEnvironment` also loses `slice` (sole use env.go:182 → `AIRA_GOVERNOR_SLICE`) and its `runtimeDir`/`diagnostics`; and removing the `runtimeDir` early-return (env.go:147) means the scope id newly exports on RuntimeDir-less launches — a second benign behaviour improvement that should be stated like the first.
8. **§10 undercounts.** governor.go also reads `AIRA_GOVERNOR_RAM_LOW_MARK`/`AIRA_GOVERNOR_RAM_HIGH_MARK`/`AIRA_GOVERNOR_RAM_SKIP_BOUND` (governor.go:163–197) plus `AIRA_SCHED_MODE` — 13 env vars retired, not 9. All daemon-side; they die cleanly with the file.
9. For the record: `admitAvailable`'s test (admit_test.go:905–931) also exercised the +1-prospective-job headroom term through a second seam; that math stays covered by grant-path tests (the test's own comment cross-references "the grant-time 44"), so the deletion loses no unique coverage.

## WAKE-PATH PROOF: **HOLDS**

Evidence checked, per consumer: (1) the admit queue evaluator selects over `kick`/`ticker.C`/`stop` and self-signals every tick (admit.go:1361–1372; `defaultAdmitPollInterval` 250 ms, paths.go:43) — the ceiling never signalled the queue, only `s.governor`; (2) grant delivery (`grantedCh` close at admit.go:1591/1678/1757, awaited at :1210) happens only inside `evaluateAdmitQueue`, i.e. on that ticker; (3) worker-admit is a pure `time.After` poll, 200 ms default, no kick channel at all (worker_admit.go:919–922, 989–995), and `admitEffectiveMaximum` is deliberately not applied to it (sliceceiling.go:562–566); (4) the CPU gate is consulted only inside `evaluateWorkerAdmit` and counts live workers from a cgroup scan (cpuslots.go:210); (5) the AIRA-101 exclusive/drain state machine and the AIRA-59 freeze both live inside `evaluateAdmitQueue`, and the holder-release path retains `queue.signal()` (admit.go:1809); (6) the only condition variable in the daemon is the eject-lifecycle `projectCond` (eject.go:125–128), unrelated; (7) `governorSet`'s own `grant`/`epoch` waiters are fed solely by `governorConnection` (governor.go:648), whose only client is the deleted relay, and its evaluator goroutine is spawned inside `newGovernorSet` (governor.go:105) — all deleted as one unit; (8) the confine client blocks on the admit connection, which the ticker-driven evaluator services; its #71 waiting message is a client-side timer. The two removed calls (sliceceiling.go:421, 447) plus admit.go:1813 and server.go:304 are the complete production `signal()` call-site set; nothing poll-free remains that the governor kick was keeping alive.

## OPEN QUESTIONS

1. **Delete `admitAvailable`.** Sole production caller is governor.go:388 (verified); a retained helper documented as "the governor's read-only advisory" (sliceceiling.go:554) would be exactly the stale claim this repo treats as a defect.
2. **`confine-reserve` KEPT — confirmed.** Spec §3.8 says "the pytest call sites" verbatim (read in source); the daemon's `parentScopeID` machinery is load-bearing for drain convergence (admit.go:224–232) and AIRA-68's reaper carve-out (confine_reaper.go:105+), so deleting the verb would orphan retained, tested paths.
3. **Keep slice1 — confirmed.** `ConfineCPUWeightAging` is live retained code (confine_linux.go:741); slice1 is its only design record; the two-line status banners are the right shape. Spec §3.8's "three specs" is simply wrong by one and the deviation is properly recorded.
4. **Rename — yes.** Verified `admit_governor_contention_test.go` never touches `governorSet` (grep empty); the stale name is precisely what would get a live AIRA-59 regression deleted by a future cleanup.
5. **Silent drop acceptable, no warning.** Only in-tree readers are governor.go:116 and server.go:296; nothing in install or unit templates sets `AIRA_SCHED_MODE` (repo-wide grep); the no-compat rule applies, and a startup warning for a subsystem that no longer exists would itself be clutter. Note the removal in the PR description and move on.
